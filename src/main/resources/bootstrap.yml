spring:
  application:
    name: ms-gateway
  cloud:
    config:
      uri: http://localhost:8888
      username: admin
      password: nttdatacorazon
  config:
    import: optional:configserver:http://localhost:8888
